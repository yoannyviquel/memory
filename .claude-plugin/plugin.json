{
  "name": "memory",
  "version": "0.1.2",
  "description": "Mémoire persistante pour Claude Code : capture brute via hooks (SessionStart/UserPromptSubmit/PostToolUse/Stop/SessionEnd), recherche hybride BM25 + sémantique (sqlite-vec + embeddings locaux transformers.js) via MCP. Stockage SQLite local embarqué — zéro serveur, zéro démon, zéro cloud.",
  "author": {
    "name": "yoannyviquel"
  },
  "mcpServers": "./.mcp.json",
  "commands": "./commands/",
  "userConfig": {
    "embedTier": {
      "type": "string",
      "title": "Palier d'embedding sémantique",
      "description": "light = e5-small (384 dim, ~120 Mo, rapide) · medium = e5-base (768 dim, ~280 Mo, meilleur compromis FR) · heavy = e5-large (1024 dim, ~560 Mo, qualité max). Modifiable ensuite via /memory:config.",
      "required": false,
      "default": "light"
    }
  }
}
